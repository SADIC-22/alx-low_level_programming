#include "main.h"
/**
 * print_rev - printing a string in reverse
 * @s: the string to reverse print
 */

void print_rev(char *s)
{
	int x, y;

	y = 0;

		while (s[y] != '\0')

		for (x = y - 1; a >= 0; x--)
		{
			_putchar(s[x]);
		}
			_putchar('\n');
}
